dist/
node_modules/
pnpm-lock.yaml

# Generated by drizzle-kit and rewritten on every generate — reformatting it would
# produce a diff on the next run and teach nobody anything.
apps/server-side/migrations/meta/

# Prose, not code. Line breaks here are deliberate, an ADR is frozen after merge so
# reformatting one is an edit to it, and Prettier's markdown rules change emphasis
# markers for no benefit anyone can name.
*.md

# Comments carry the reasons for these files and Prettier moves quotes around
# without improving anything; the churn would bury the next real change.
docker-compose.yml
Dockerfile.dev
