{
  "name": "expansa",
  "version": "0.0.0",
  "private": true,
  "description": "Expansa \u2014 MMO strategy game",
  "packageManager": "pnpm@11.17.0",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --force",
    "test": "pnpm -r test",
    "db:migrate": "pnpm --filter @expansa/platform migrate",
    "db:generate": "drizzle-kit generate"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "drizzle-kit": "^0.31.6"
  }
}
