{
  "name": "@expansa/platform",
  "version": "0.0.0",
  "private": true,
  "description": "Contour 1 — adapters over adopted technology, and the machinery they share",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "typecheck": "tsc -b --force tsconfig.json",
    "migrate": "tsc -b tsconfig.json && node dist/database/migrate.js"
  },
  "dependencies": {
    "@expansa/kernel": "workspace:*",
    "drizzle-orm": "^0.44.7",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "@types/pg": "^8.15.6",
    "drizzle-kit": "^0.31.6",
    "typescript": "^5.9.3"
  }
}
